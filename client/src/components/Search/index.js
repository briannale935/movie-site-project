import React from 'react';
import AppBar from '../AppBar';

const Search = () => {
    return (
        <div>
            <AppBar />
            <h1>Search Page</h1>
        </div>
    );
};

export default Search;
