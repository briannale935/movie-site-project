import React from 'react';
import AppBar from '../AppBar';

const MyPage = () => {
    return (
        <div>
            <AppBar />
            <h1>My Page</h1>
        </div>
    );
};

export default MyPage;
